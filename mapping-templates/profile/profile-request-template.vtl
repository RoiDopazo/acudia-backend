#if ($ctx.identity.username)
  #set($ID = $ctx.identity.username)
#else
  #set($ID = "")
#end

#set($PK_value = "#$ID")
#set($PK = "$ctx.args.input.role$PK_value")

{
    "version" : "2017-02-28",
    "operation" : "GetItem",
    "key" : {
        "PK" : {
            "S" : "$PK"
        },
        "SK" : {
            "S" : "PROFILE"
        }
    },
    "consistentRead" : true
}
